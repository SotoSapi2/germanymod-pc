file(
	GLOB_RECURSE 
	GLOB
	CONFIGURE_DEPENDS 
	"${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp" 
	"${CMAKE_CURRENT_SOURCE_DIR}/src/*.c"
)

add_library(fileutils STATIC ${GLOB})
target_include_directories(fileutils PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/src")